javascript:(function(){    var script = document.createElement('script');    script.src = 'https://cdn.jsdelivr.net/gh/Famoriel1/My-Panel/jspanel.js';    script.onload = function() {        var extensions = [            'contextmenu/jspanel.contextmenu.js',            'hint/jspanel.hint.js',            'modal/jspanel.modal.js',            'tooltip/jspanel.tooltip.js',            'dock/jspanel.dock.js',            'layout/jspanel.layout.js'        ];        var loadExtensions = function(index) {            if (index < extensions.length) {                var extensionScript = document.createElement('script');                extensionScript.src = 'https://cdn.jsdelivr.net/gh/Famoriel1/My-Panel/extensions/' + extensions[index];                extensionScript.onload = function() {                    loadExtensions(index + 1);                };                document.head.appendChild(extensionScript);            } else {                executePanelCreation();            }        };        loadExtensions(0);    };    document.head.appendChild(script);    function executePanelCreation() {        jsPanel.create({            headerTitle: 'Panel',            theme: 'dark',            content: generateButtons(10),            callback: function() {                this.content.style.padding = '10px';                const buttons = this.content.querySelectorAll('button');                buttons.forEach(button => {                    button.addEventListener('click', handleButtonClick);                });            }        });        function generateButtons(count) {            let buttonsHTML = '';            for (let i = 1; i <= count; i++) {                buttonsHTML += `<button id="button${i}" style="color: white; background-color: black; border-radius: 7px; margin-right: 10px; margin-bottom: 10px;"> Button ${i} </button>`;            }            return buttonsHTML;        }        function handleButtonClick(event) {            const buttonId = event.target.id;            const codeSnippets = {                button1: 'console.log("Code for button 1");',                button2: 'alert("Code for button 2");',            };            if (codeSnippets[buttonId]) {                eval(codeSnippets[buttonId]);            }        }    }})();